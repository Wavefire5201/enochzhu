node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# docs
docs/
# anchored: an unanchored `notes/` also matches src/routes/notes, src/content/notes
# and static/notes, and tailwind v4's source scanner honours .gitignore — every
# class used only on the notes pages silently stops being generated
/notes/
references/
/assets/

# ai tooling
.omc/
.playwright-mcp/

src/routes/proto/

# hero pipeline previews (scripts/hero/backdrops.py --compare)
scripts/hero/out/

# proto depth-model comparison maps (scripts/hero, throwaway)
static/hero/depth/proto/
